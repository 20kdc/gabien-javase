apply plugin: "java"
apply plugin: "eclipse"
apply plugin: "idea"

dependencies {
    compile files('../gabien-common/build/libs/gabien-common.jar')
}

sourceCompatibility = 1.6
targetCompatibility = 1.6

group = "gabien"
archivesBaseName = "gabien-javase"
